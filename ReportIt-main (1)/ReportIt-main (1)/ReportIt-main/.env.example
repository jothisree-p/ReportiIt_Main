# Recommended for local dev (avoids CORS; matches Postman targets via proxy)
VITE_USE_PROXY=true
VITE_AUTH_URL=
VITE_API_URL=

# Direct URLs (same as Postman) — only if VITE_USE_PROXY=false
# VITE_USE_PROXY=false
# VITE_AUTH_URL=http://localhost:8081
# VITE_API_URL=http://localhost:8082
